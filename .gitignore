__pycache__/
*.pyc
.env
.DS_Store
data/scraper.db
data/scraper.db-shm
data/scraper.db-wal
data/exports/
data/schedules/
server/static/screenshots/
screenshot_cache/
degraded_dump/
worker/degraded_dump/
runtime_settings.json
*.egg-info/
.venv/
# 约定用 .venv（README 与 deploy/setup.sh 都是它）。这里连 venv/ 一起忽略，
# 是因为不带点的写法太常见 —— 漏掉它意味着满目录文件变成 untracked，
# 一个手滑的 `git add -A` 就能把整个虚拟环境提交进来。
venv/
env/
.agent/runtime_logs/
.agent/evidence/

# Runtime monitor artifacts
.agent/monitor/*.jsonl
.agent/monitor/*_state.json

# Claude Code 给 subagent 开的隔离 git worktree（isolation: "worktree"）。
# 每个都是整个仓库的一份副本，是临时的、workflow 结束会自动清掉 ——
# 只忽略 worktrees/ 而不是整个 .claude/：将来真要提交
# .claude/settings.json 或 skills 时，它们还能正常出现在 git status 里。
.claude/worktrees/
